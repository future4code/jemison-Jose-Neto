```function calculaPrecoTotal(quantidade) {

  if (quantidade >= 12){
    const valorCompra = quantidade * 1
    return valorCompra
  } else {
    const valorCompra = quantidade * 1.3
    return valorCompra
  }
  return valorCompra
}```